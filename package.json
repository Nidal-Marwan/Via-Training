{
  "name": "training",
  "version": "1.0.0",
  "description": "monorepo for drivers-map website that contain the client and server projects",
  "main": "index.js",
  "scripts": {
    "start:server:dev": "pnpm run start:dev --filter training-server",
    "start:server:prod": "pnpm run start:prod --filter training-server",
    "start:client:dev": "pnpm run start:dev --filter training-client",
    "start:client:prod": "pnpm run start:prod --filter training-client",
    "start:client:server": "pnpm run build:client --filter training-server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nidal-Marwan/Via-Training.git"
  },
  "homepage": "https://github.com/Nidal-Marwan/Via-Training#readme"
}
